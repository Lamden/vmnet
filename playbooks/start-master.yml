- name: start nodes
  sudo: yes
  hosts: master
  gather_facts: false

  tasks:
     - name: copy files
       copy: src=/vagrant/scripts dest=/home/vagrant
     - name: Remove debug.log
       file:
         state: absent
         path: debug.log
     - name: Start master
       command: python3 /home/vagrant/group-server.py